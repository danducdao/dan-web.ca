import { FiltreCategoryPipe } from './filtre-category.pipe';

describe('FiltreCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
